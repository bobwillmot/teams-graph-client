# Architecture Decision Records

Long-term design decisions and tradeoffs for this project live in this folder.

Conventions:

- Use lowercase hyphenated Markdown names by default, while keeping `README.md` uppercase for folder indexes.
- Use numbered ADR files such as `0001-short-title.md`.
- Keep the standard sections: `Status`, `Context`, `Decision`, and `Consequences`.
- Add a new ADR when a decision changes the project's long-term shape or operating model.

Current records:

- [0001-initial-graph-client-foundation](./0001-initial-graph-client-foundation.md)